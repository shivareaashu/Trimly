import PlaceholderPage from '@/components/admin/PlaceholderPage.jsx';

export default function WebsitePage() {
  return (
    <PlaceholderPage
      eyebrow="Public Presence"
      title="Website"
      description="Manage the salon website, booking surface, templates, publishing status, and public content."
      items={['Website builder shell', 'Template controls', 'Publishing workflow']}
    />
  );
}
